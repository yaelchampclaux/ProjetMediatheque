<form method="post" action="{{ path('type_auteur_delete', {'id': type_auteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_auteur.id) }}">
    <button class="btn">Delete</button>
</form>
