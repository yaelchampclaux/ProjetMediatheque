<form method="post" action="{{ path('type_oeuvre_delete', {'id': type_oeuvre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_oeuvre.id) }}">
    <button class="btn">Delete</button>
</form>
